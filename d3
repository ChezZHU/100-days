
# 程序将千米转换为英里
# 距离将由用户输入

km = float(input('输入距离(千米): '))

km_miles_ratio = 0.621

miles = km * km_miles_ratio
print('转化为英里为:', miles)


# 请在 ___ 划线处，替换成你的代码

# 从用户那里获得温度（摄氏度），并赋值给celsius
celsius = float(input())

# 用公式将其转换为华氏度数
fahrenheit = (celsius * 1.8)+32

# 打印华氏度数，以华氏为单位
print(fahrenheit)



# 请在 ___ 划线处，替换成你的代码

# 创建变量 fee 和 discount_percent
fee = 1536
discount_percent = 10/100
# 计算折扣并将其分配给变量discount
discount = fee * discount_percent

# 计算并打印你需要支付的学费
fee_pay = fee - discount
print (fee_pay)

# 获取字符串 'Pikachu' 的类型
data_type = type('Pikachu')

print(data_type)

# 请在 ___ 划线处，替换成你的代码

# 创建变量 x 和 y
x = '5'
y = '10'

# 将储存在x和y中的数值转换为整数，并将其相乘
x1=int(x)
y1=float(y)

# 打印结果
print(x1*y1)

var = '25.0'
numer = float(var)
print(numer)



# 请在 ___ 划线处，替换成你的代码

# 从用户那里获得两个输入，并将其转换为浮点数
# 然后将它们分配给变量number1和number2
number1 = float(input('请输入Number1: '))
number2 = float(input('请输入Number2: '))

# 将数字相加，结果存在result
result = number1 + number2


# 打印result的平方
print(result**2)

number = 25

square_root = number ** (1/2)
print(number,"的平方根是", square_root)


# 请在 ___ 划线处，替换成你的代码

# 从用户那里获得三角形的三条边
# 将它们存储在变量a、b和c中
float
a = float(input())
b = float(input())
c = float(input())
# 计算半周长 s
s = (a + b + c)/2
# 计算面积并打印 
area = (s * (s - a) * (s - b) * (s - c)) ** 0.5
print(area)

# 比较操作符
# 以下是python中所有的比较操作符：

# 操作符	表达式	备注
# >	var > 10	判断 var是否大于 10
# >=	var >= 10	判断 var是否大于或等于 10
# <	var < 10	判断 var是否小于 10
# <=	var <= 10	判断 var是否小于或等于 10
# ==	var == 10	判断 var是否等于 10
# !=	var != 10	判断 var是否不等于 10
# 比较操作符运算的结果，都是逻辑值(布尔值)，返回True 或 False。

# 逻辑运算符
# 前面我们学习了逻辑表达式，现在将学习逻辑运算符。下面是三个逻辑运算符。

# 操作	解释	例子
# and	如果两个同时为真返回真	True and True
# or	如果两个同时为假返回假	False or False
# not	取反	not True



# 获取三个整数 
n1 = int(input())
n2 = int(input())
n3 = int(input())

# 如果n3是最小的数字，则打印True，否则打印False
print(n3 < n1 and n3 < n2) 

